# Contributing

1. Open an issue describing the change.
2. Keep changes focused and add regression tests.
3. Run `npm ci`, `npm run typecheck`, `npm test`, and `npm run build`.
4. Do not commit credentials, runtime databases, transcripts, or private endpoints.
5. Use Conventional Commit messages.

Security reports belong in private vulnerability reporting, not public issues.
