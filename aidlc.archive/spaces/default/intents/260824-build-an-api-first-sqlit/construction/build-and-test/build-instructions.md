# Build Instructions

## Commands
```bash
npm install
npm run typecheck
npm run build
```

TypeScript output is generated in `dist/`. No additional environment variables are required for compilation.
