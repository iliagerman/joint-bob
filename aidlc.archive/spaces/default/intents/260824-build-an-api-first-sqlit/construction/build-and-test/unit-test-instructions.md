# Unit Test Instructions

No automated unit-test suite is configured. Static verification used:

```bash
node --check public/app.js
node --check public/sw.js
```

Both files must parse without errors.
